// ltl-pdf-text/main.ltl
//
// Minimal PDF text extractor — pages, content streams, CMap.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-pdf-text v0.1.0")
    io::println("Minimal PDF text extractor — pages, content streams, CMap.")
    0
}
